export interface IconProps {
    src: string;
    alt: string;
    className?: string;
  }
  
  export interface SearchBarProps {
    onSearch?: (value: string) => void;
  }
  
  export interface NavigationIconsData {
    src: string;
    alt: string;
    className?: string;
  }